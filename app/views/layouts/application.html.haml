!!!
%html{ lang: 'en' }
  %head
    %meta{ content: 'text/html; charset=UTF-8', http: { equiv: 'Content-Type' } }
    %meta{ charset: 'utf-8' }
    %meta{ content: 'IE=edge', http: { equiv: 'X-UA-Compatible' } }
    %meta{ content: 'width=device-width, initial-scale=1', name: 'viewport' }
    %meta{ content: (yield(:keywords).presence || AppSettings.main[:meta][:keywords]), name: 'keywords' }
    %meta{ content: (yield(:description).presence || AppSettings.main[:meta][:description]), name: 'description' }

    %title
      = yield(:title).presence || AppSettings.main[:meta][:title]

    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'

    = csrf_meta_tags

    = favicon_link_tag

    / HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries
    / WARNING: Respond.js doesn't work if you view the page via file://
    /[if lt IE 9]
      = javascript_include_tag javascript_path('https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js')
      = javascript_include_tag javascript_path('https://oss.maxcdn.com/respond/1.4.2/respond.min.js')

  %body
    .container
      = render 'shared/topmenu'
      = render 'shared/header'
      = render 'shared/submenu'
      = render 'shared/flashes'
      = yield
      %hr